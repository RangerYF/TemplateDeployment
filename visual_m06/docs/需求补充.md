增加撤销重做功能，ctrl+z撤销，ctrl+y重做

2D不需要旋转，但是交互不能少；看看visual_template里面
  的交互都有哪些。包括选中向量等等。添加一个演示台，在这个模式下，完全参考template里面的实现
  ，比如创建向量，悬停变色选中向量之后显示向量信息，可以修改向量信息，并且可以在这里进行数乘
  ；上方出现类似template的那种“选择”“创建向量”“向量运算”框，点击向量运算之后可以选两个向量进
  行加法、减法、数量积等等，向量、运算都作为实体，类似visual_template，并且支持导入导出。

数乘的向量不能是创建一个新向量，而要和原向量始终保持数乘关系，原向
量不改变就不能单独改变数乘向量

 向量列表显示的属性需要可以点击编辑。显示x y
  分量和角度，如果修改角度就是保持长度不变修改。向量运算后得到的向
  量需要和原向量绑定关系，只有修改原向量才能导致运算后的向量同步改
  变（但是可以拖拽移动这个向量的起点）。

  我需要UI和组件尽最大可能贴近visual_template中的实现，包括组件位置、字体大小。请你阅读visual_template仓库，按照其UI修改我的UI。

  

