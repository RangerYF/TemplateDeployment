# 阶段 8.6：收尾交付（含环境参数与速度可视化教学增强）

- **任务ID**: 03-31-23-55-stage8.6-environment-velocity
- **风险等级**: L1（常规风险：多文件联动，UI/状态/渲染一致性改动）
- **当前文档动作风险**: L0（仅任务文档修订与阶段文档同步）
- **状态**: 已完成（8.6.1-8.6.5 全部完成，验收与交付 DoD 已在本阶段收口）
- **前置依赖**: 8.5 ✅
- **规则来源**: 用户显式指令（删除“高中教学环境参数扩展”并纳入原 8.6 规划）
- **流程路径**: MODE 0 -> MODE 3 -> MODE 4 -> MODE 5 -> MODE 6（实施阶段）

## 说明

- 仓库中未找到 `.tasks/task_evaluation_framework.md`；本任务风险分级与流程路由按 AGENTS 的 RIPER-7 风险矩阵执行。

## 用户原始需求（归档）

1. 当没选中任何物体的时候，显示当前环境配置项；环境要允许配置重力加速度（高中场景有时用 `10`，有时用 `9.8`）。
2. 当前初速度设置似乎只能设置分速度，不能设置合速度；设置合速度后看不到速度方向。跨步中若设置了速度，需要速度方向说明（箭头与文字）。

## 目标

按原 8.6 收尾范围完成视觉统一、响应式性能、边界与验收，同时纳入“环境参数与速度可视化教学增强”。

## 子任务拆分（串行执行）

| 子任务 | 目标 | 详细文档 | 状态 |
|---|---|---|---|
| 8.6.1 | 无选中态环境配置面板 + `g` 配置生效 | `stage-8.6.1-environment-panel.md` | ✅ |
| 8.6.2 | 初速度“合/分速度”双向联动 + 速度箭头文本（含初始加速度扩展） | `stage-8.6.2-velocity-ux.md` | ✅ |
| 8.6.3 | 视觉统一剩余项收敛 | `stage-8.6.3-visual-unification.md` | ✅ |
| 8.6.4 | 响应式与性能优化（1920×1080/1280×720/20+物体） | `stage-8.6.4-responsive-performance.md` | ✅ |
| 8.6.5 | 边界情况与异常处理收口 | `stage-8.6.5-boundary-exception.md` | ✅ |

## 执行规则

1. 严格按 `8.6.1 -> 8.6.2 -> 8.6.3 -> 8.6.4 -> 8.6.5` 串行执行。
2. 每完成一个子任务，必须先做 MODE 5 审查与门禁验证，再进入下一个子任务。
3. 若执行中出现计划外改动，按规则回到 MODE 3 更新对应子任务文档后再继续。

## 涉及文件（预估）

- `src/components/layout/`
- `src/components/`
- `src/components/Canvas.tsx`
- `src/components/panels/PropertyPanel.tsx`
- `src/store/sceneStore.ts`
- `src/store/editorStore.ts`
- `src/renderer/`
- `src/renderer/CanvasRenderer.ts`
- `src/engine/`
- `src/engine/PhysicsBridge.ts`
- `src/styles/`
- `src/models/`（速度字段表达与类型约束，按实际命中）

## 验收标准

- `1920×1080` 下界面完整美观。
- `1280×720` 下布局不破碎，核心功能可用。
- 20 个物体同时仿真无明显卡顿。
- 空场景/错误操作/异常仿真均有明确反馈。
- 教师可独立完成完整教学演示流程。
- 未选中任何物体时，右侧可见环境配置，`g=9.8/10/自定义` 可切换并生效。
- 初速度支持“合速度/分速度”双向设置，且可见方向箭头与文字说明。
- 输入异常有明确反馈，不产生 NaN 或渲染异常。

## 最小回归与验证计划（实施阶段）

- 命中目录：`src/`
- 门禁命令：`pnpm lint && pnpm tsc --noEmit`
- 交互手测：
  - 无选中态环境配置显示与 `g` 切换
  - 合速度/分速度互转一致性
  - 速度矢量在编辑态、播放态、回放态可读
  - 异常输入（空值/非法字符/极值）处理
  - `1920×1080` 与 `1280×720` 两档视口验收

## 本轮文档修订记录

- 已删除“高中教学环境参数扩展（最小集合）”整块。
- 已纳入原 8.6 阶段规划的四项主线（视觉统一/响应式性能/边界处理/端到端验收）。
- 已拆分为 5 个可串行执行的子任务文档，供“一个一个完成”。

## 子任务进展补记（2026-04-01）

### 8.6.3 视觉统一收敛已完成

- 规则来源：用户显式执行指令（“开始这个任务”）+ 8.6 总任务拆分。
- 主要产出：
  - 新增统一视觉 token：`EDITOR_CHROME / FEEDBACK_VISUAL / CONSTRAINT_VISUAL`
  - 收敛面板/工具栏控件基线（字号、间距、边框、阴影）
  - 收敛约束线、力箭头、hover/selected、对齐/吸附反馈参数
- 验证结果：
  - `pnpm lint && pnpm tsc --noEmit` 通过
- 详细记录：`stage-8.6.3-visual-unification.md`

### 8.6.4 响应式与性能优化已完成

- 规则来源：用户显式执行指令（“先审查性能并直接优化；随后直接补响应式”）。
- 主要产出：
  - 仿真主循环减少重复状态读取与对象分配（`bodyStates` 单帧复用）
  - 力渲染改为位姿覆盖链路，移除仿真态每帧 `sceneBodies` 克隆
  - 左右侧栏改为 `clamp(...)` 宽度，工具栏与分析面板完成窄屏降级策略
- 验证结果：
  - `pnpm lint && pnpm tsc --noEmit` 通过
- 详细记录：`stage-8.6.4-responsive-performance.md`

### 8.6.5 边界处理与异常收口已完成（含续作）

- 规则来源：用户显式缺陷修复指令（3 个具体 bug）。
- 主要产出：
  - 修复弹簧压缩场景受力方向错误（压缩外推、拉伸内拉）。
  - 修复滚轮缩放中心偏移（改为严格鼠标中心缩放）。
  - 修复设备模拟下拖拽创建不稳定（drop 类型兜底 + 触摸点击创建兜底）。
  - 完成 P0+P1：仿真熔断、约束零除防护、删除级联恢复、错误提示通道、空场景引导、输入校验一致化。
  - 空态引导样式续作：文案居中、按钮切换为统一 Button 规范、去除卡片外边框。
- 关联决策：
  - 设备模拟下“拖拽跟手一致性（Pointer 自定义拖拽链路）”暂不在本轮实施，已记录至 `TODO.md` 作为后续优化项。
- 验证结果：
  - `pnpm lint && pnpm tsc --noEmit` 通过
- 详细记录：`stage-8.6.5-boundary-exception.md`

## 阶段收口结论（2026-04-01）

- 8.6 阶段已整体完成，子任务 `8.6.1-8.6.5` 全部闭环。
- 原计划中的“端到端验收与交付 DoD”不再单独拆分文档，改为并入本阶段收口记录。
