# 3.5-阶段任务文档：P-14 机械能守恒（A类模板）

- 所属计划：`.tasks/active/template/PROGRESSIVE-PLAN.md`
- 阶段编号：`T3.5`
- 模块：`P-14`
- 风险等级：`L2`（能量场景参数映射与仿真验收）
- 当前状态：`已完成（2026-04-02）`

---

## 目标
完成 `P-14` 模块 A 类模板纵向交付，确保重力势能与动能转化场景可稳定复现。

## 覆盖模板（7个）
- `ENE-001`、`ENE-002`
- `ENE-011`
- `ENE-021`、`ENE-022`
- `ENE-041`
- `ENE-051`

## 执行方式
1. 按“单体势能转化 -> 斜面/曲面运动 -> 组合场景”顺序推进。
2. 模板中显式配置摩擦、恢复系数与初始高度，避免现象偏离。
3. 每个模板都做能量趋势观察，确认教学可解释性。

## 单模板标准流程（强制）
1. 在 `src/templates/presets/p14-energy/*.json` 编写命令程序。
2. 运行 `pnpm run templates:generate-json` 生成 JSON。
3. 在 `src/templates/catalog-data/templates.json` 注册为 `ready`。
4. 用生成阶段一致性门禁 + 运行态 sanity 完成校验。
5. 工作台实点进入编辑器，完成加载与仿真验收。

## 回归门禁（命中 `src/`）
- `pnpm lint`
- `pnpm tsc --noEmit`

## 验收清单
- [x] 模板现象满足机械能守恒教学主线（考虑摩擦时符合预期衰减）。
- [x] 参数映射正确（质量/摩擦/高度/速度）。
- [x] 加载、编辑、仿真流程稳定。
- [x] lint 与类型检查通过。

## 模板进度看板
- [x] `ENE-001`
- [x] `ENE-002`
- [x] `ENE-011`
- [x] `ENE-021`
- [x] `ENE-022`
- [x] `ENE-041`
- [x] `ENE-051`

## 风险与回滚
- 风险：能量图表趋势与场景现象可能不一致，影响教学可信度。
- 控制：逐模板记录关键观测量并与场景参数交叉校验。
- 回滚：问题模板先回退到 `planned`，待参数修正后二次上架。

## 本次执行记录（2026-04-02）
- 用户原文：`继续3.4和3.5`
- 规则裁决：`用户显式指令 > 风险分级自动流转 > 当前模式默认路径`
- 执行策略：按 `S10/S05/S02/S06/S03` 母版顺序推进，先落体与抛体，再补双体耦合、斜面对比、弹簧与单摆能量模板。

### 数据契约样例（L2 强制）
- 运行态模板目录：`src/templates/catalog-data/templates.json`
  - 关键字段：`meta.id/module/status`、`sceneSource`、`sceneJsonPath`、`teaching`
- 生成态模板目录：`src/templates/presets/p14-energy/*.json`
  - 关键字段：`commandProgram.templateId/sceneName/version/commands[]`
  - 策略位：`allowNonZeroInitialVelocity`（仅 `ENE-002` 显式开启）

### 本轮交付
- 由 `planned` 升级为 `ready`：`ENE-001`、`ENE-011`、`ENE-041`
- 新增并接入 `ready`：`ENE-002`、`ENE-021`、`ENE-022`、`ENE-051`
- 新增场景 JSON：
  - `public/templates/scenes/ENE-001.json`
  - `public/templates/scenes/ENE-002.json`
  - `public/templates/scenes/ENE-011.json`
  - `public/templates/scenes/ENE-021.json`
  - `public/templates/scenes/ENE-022.json`
  - `public/templates/scenes/ENE-041.json`
  - `public/templates/scenes/ENE-051.json`

### 验证证据
- `pnpm run templates:generate-json`：通过（含 `ENE-001/002/011/021/022/041/051`）
- `pnpm lint`：通过
- `pnpm tsc --noEmit`：通过
- 备注：脚本执行中出现 Vite `EPERM 0.0.0.0:24678` 日志，但未影响模板生成与门禁结果。

## 追加维护记录（2026-04-02）
- 用户原文：`工作台,布局改一下 顶部用一个tab,放所有模块`
- 规则裁决：`用户显式指令 > 风险分级自动流转 > 当前模式默认路径`
- 改动决策：工作台由“按模块分段纵向展示”改为“顶部模块 Tab 切换 + 单内容区展示当前模块模板卡片”，仅展示 `ready` 模板。
