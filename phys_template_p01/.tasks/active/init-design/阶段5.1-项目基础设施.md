# 阶段5.1：项目基础设施

> 批次1/6 | 依赖：无 | 包含：T1项目初始化 + T2 Git配置 + T3 Husky/ESLint/Prettier

---

## 上下文

本子任务是阶段5（项目初始化与骨架搭建）的第一批，是**整个项目首次编写实际代码**。完成后项目具备完整的开发工具链，后续子任务在此基础上编写业务代码。

## 实施任务

### T1: 项目初始化（包管理器 + 构建工具 + TypeScript 配置）

**需要创建的文件**：
- `package.json` — 项目元信息、scripts、dependencies
- `tsconfig.json` — TypeScript strict 配置（target ES2020, strict true, noEmit true, moduleResolution bundler, jsx react-jsx）
- `vite.config.ts` — Vite 6 + React 插件
- `index.html` — Vite 入口 HTML
- `src/main.tsx` — React 应用入口（最小可运行：渲染一个空 div）

**scripts 约定**：
```json
{
  "dev": "vite",
  "build": "tsc --noEmit && vite build",
  "lint": "eslint src/ --ext .ts,.tsx",
  "preview": "vite preview"
}
```

**依赖清单**（参照技术栈选型结论 4.5 和 design_guid 移植指南）：
- dependencies：react, react-dom, zustand, class-variance-authority, clsx, tailwind-merge, lucide-react, @radix-ui/react-slot（以及 shell/ 需要的 Radix 组件）
- devDependencies：vite, @vitejs/plugin-react, typescript, @types/react, @types/react-dom, tailwindcss, postcss, autoprefixer, eslint, @typescript-eslint/eslint-plugin, @typescript-eslint/parser, eslint-plugin-react-hooks, prettier

**设计关注点**：
- [x] Tailwind 配置从 `design_guid/tailwind.config.ts` 迁移/引用，保留 EduMind 色彩 token
- [x] `postcss.config.js` 配置 Tailwind 插件
- [x] `src/styles/` 目录复制或引用 `design_guid/styles/` 下的 token 文件

### T2: Git 配置

**需要创建/确认的文件**：
- `.gitignore` — node_modules, dist, .env*, .DS_Store, *.local 等

**设计关注点**：
- [x] 确认 Git author 为 `cjn <1229412289@qq.com>`（检查 `git config user.name` 和 `git config user.email`）
- [x] 如不匹配需配置 repo-level config（已修正 email 从 `cjn@xx.com` → `1229412289@qq.com`）

### T3: Husky pre-commit hook + ESLint/Prettier 配置

**需要创建的文件**：
- `.husky/pre-commit` — hook 脚本
- `.eslintrc.cjs`（或 `eslint.config.js`）— ESLint 配置
- `.prettierrc` — Prettier 配置

**hook 命令**：
```bash
pnpm lint && pnpm tsc --noEmit
```

**设计关注点**：
- [x] ESLint 规则：`@typescript-eslint/recommended` + `react-hooks/recommended`
- [x] Prettier 与 ESLint 协同（`eslint-config-prettier` 关闭冲突规则）
- [x] CLAUDE.md 附录F门禁矩阵已有 `pnpm lint && pnpm tsc --noEmit`，与实际 scripts 一致，无需修改

---

## 验收标准

- [x] `pnpm dev` 能启动空白页 — Vite 6.4.1 ready in 394ms
- [x] `pnpm lint && pnpm tsc --noEmit` 通过
- [x] Git author 配置为 `cjn <1229412289@qq.com>`
- [x] pre-commit hook 已配置（`pnpm lint && pnpm tsc --noEmit`）

## 完成记录

- **完成时间**：2026-03-17
- **实际产出文件**：
  - `package.json` — 含 pnpm.onlyBuiltDependencies 配置
  - `tsconfig.json` — strict + 路径别名 `@/*`
  - `vite.config.ts` — Vite 6 + React + 路径别名
  - `postcss.config.js` — tailwindcss + autoprefixer
  - `tailwind.config.ts` — EduMind 色彩 token（去掉 `@tailwindcss/typography`，Phase 1 不需要）
  - `index.html` — Vite 入口
  - `src/main.tsx` — 最小可运行 React 入口
  - `src/styles/` — 从 design_guid 完整迁移 5 个 token 文件 + `globals.css`
  - `src/lib/utils.ts` — cn() 类名合并工具
  - `.gitignore`
  - `.eslintrc.cjs` — ESLint 配置
  - `.prettierrc` — Prettier 配置
  - `.husky/pre-commit` — pre-commit hook

## 参考文档

| 文档 | 用途 |
|------|------|
| `docs/design/技术栈选型结论.md` | 依赖列表、TS 配置、CSS 方案 |
| `design_guid/DESIGN_SYSTEM_GUIDE.md` | Tailwind 配置、依赖列表 |
| `design_guid/tailwind.config.ts` | Tailwind 色彩 token 来源 |
