# 阶段5.2：设计系统集成 + 核心类型定义 ✅

> 批次2/6 | 依赖：5.1已完成 | 包含：T4设计系统集成 + T5 core/types.ts
> **完成时间**：2026-03-17

---

## 上下文

5.1已搭建完整开发工具链。本批次将设计系统迁入项目并转写全部51个核心类型接口，为后续所有业务模块提供类型基础。

## 实施任务

### T4: 设计系统集成（design_guid → src） ✅

**已迁移的文件**：
- `src/lib/utils.ts` — cn() 类名合并工具（5.1已迁移）
- `src/styles/` — tokens/colors/typography/spacing/index + globals.css（5.1已迁移）
- `src/components/ui/` — 8个 UI 组件（本批次迁移）
- `tailwind.config.ts` — 已适配项目路径（5.1已完成）

**迁移的 UI 组件**（8个，仅参数面板所需）：
- `button.tsx` — Button（多变体）
- `slider.tsx` — Slider（自定义滑块）
- `select.tsx` — Select（下拉选择）
- `input.tsx` — Input（文本输入框）
- `label.tsx` — Label（表单标签）
- `switch.tsx` — Switch（开关）
- `tabs.tsx` — Tabs/TabsList/TabsTrigger/TabsContent（标签页）
- `card.tsx` — Card/CardHeader/CardTitle/CardDescription/CardContent/DifficultyBadge（卡片）

**迁移调整**：
- 导入路径：`@/lib/utils/cn` → `@/lib/utils`（项目 cn() 位于 `src/lib/utils.ts`）
- 移除 `"use client"` 指令（Vite SPA 不需要）
- 修复3个 strict 模式类型错误（`??` 空值合并替代 `||`，`cloneElement` 的 `as any` 改为 `as Record<string, unknown>`）
- 未迁移的组件（Phase 1 不需要）：alert, badge, checkbox, dialog, markdown, pagination, popover, progress, progress-bar, Skeleton, table, textarea, toast, ExampleCard

**设计关注点**：
- [x] 只迁移阶段5-6需要用到的 UI 组件，不全量复制
- [x] 确保 Tailwind 的 content 路径覆盖 `src/**/*.{ts,tsx}`
- [x] 验证迁移后的组件 `pnpm tsc --noEmit` 通过

### T5: core/types.ts — 核心类型定义 ✅

**创建的文件**：
- `src/core/types.ts` — 阶段2定义的全部51个 TypeScript 接口/类型（单文件方案）

**实现依据**：`核心类型系统-接口设计.md` 中8个模块的完整接口定义

**类型模块**（单文件 `src/core/types.ts`，按模块分区注释）：
1. 向量与几何（3）：Vec2, Rect, Transform
2. 实体（4）：EntityId, EntityType, EntityCategory, Entity\<T\>
3. 关系（3）：RelationId, RelationType, Relation\<T\>
4. 参数Schema（9）：ParamControlType, ParamSchemaBase, SliderParamSchema, InputParamSchema, ToggleParamSchema, SelectParamSchema, ParamSchema, ParamGroup, ParamValues
5. 物理结果（7）：ForceType, Force, OrthogonalDecomposition, ForceAnalysis, MotionState, EnergyState, PhysicsResult
6. 模拟状态（6）：SimulationStatus, SolveMode, IntegratorType, TimelineState, SceneDefinition, SimulationState
7. 视角层（10）：ViewportType, InfoDensity, ViewportState, ForceViewportData, MotionViewportData, EnergyViewportData, MomentumViewportData, FieldViewportData, CircuitViewportData, ViewportData
8. 渲染（8）：CoordinateTransform, RenderContext, HitTestResult, ArrowHeadStyle, LineStyle, VisualStyle, ForceVisualMap, PhysicsVisualMap

**设计关注点**：
- [x] 严格对照阶段2文档中的接口定义，不遗漏字段、不自行添加
- [x] 确保 `strict: true` 下类型检查通过
- [x] 导出方式：统一从 `src/core/types.ts` 导出（51个 export）

---

## 验收标准

- [x] 设计系统组件迁移后 tsc 类型检查通过
- [x] 51个类型接口完整转写（`grep -c` 验证 export 数量 = 51）
- [x] `pnpm tsc --noEmit` 通过
- [x] `pnpm lint` 通过

## 参考文档

| 文档 | 用途 |
|------|------|
| `docs/design/核心类型系统-接口设计.md` | **关键**：51个接口定义，逐一转写 |
| `design_guid/DESIGN_SYSTEM_GUIDE.md` | 设计系统迁移指南 |
| `design_guid/styles/` | 色彩/排版/间距 token 来源 |
| `design_guid/components/ui/` | UI 组件来源 |
