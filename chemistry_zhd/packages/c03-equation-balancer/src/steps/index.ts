import type { ParsedEquation } from '../parser/types';

/** 步骤内容由 main.ts buildSteps() 负责生成；此处保留空实现占位。 */
export function generateSteps(_eq: ParsedEquation, _coeffs: number[]): never[] {
  return [];
}
